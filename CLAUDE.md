<!-- CR-GUARD: do-not-develop-here -->
# ⛔ Don't launch an agent / start a work session from this folder

This is a **runtime checkout** of the project's code. Editing the code here is
fine — but don't *start* an agent session or a work arc from this directory.

Agents start in **Code Research** (the dispatch console: project registry +
startup-sync). From there you read `projects/<name>/` for design docs and edit
this repo's code via its absolute path. Design docs live in Code Research, not
in this repo — keep this checkout runtime-code + README only.
